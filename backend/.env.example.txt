# Server Configuration
PORT=5000
NODE_ENV=development

# JWT Secret (تغيير في الإنتاج)
JWT_SECRET=shamsi_tn_secret_key_2024

# Database (SQLite)
# قاعدة البيانات SQLite تنشأ تلقائياً في مجلد /data
# لا تحتاج إلى إعدادات إضافية

# API URL (للتطوير)
API_URL=http://localhost:5000/api